services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shopping-ai-agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PORT: 8000
      UVICORN_RELOAD: "false"
    restart: unless-stopped
